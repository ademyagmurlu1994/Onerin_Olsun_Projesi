export const setValueData = ({commit}, payload) => {
    commit("setValue", payload);
}

export const login = ({ commit, dispatch, state}, authData) => {  //(vuexContext)
    
}
export const logout = ({ commit, dispatch, state}) =>{

}